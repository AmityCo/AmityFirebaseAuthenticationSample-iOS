# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'google-identity-sign-in' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for google-identity-sign-in
  pod 'FirebaseAnalytics'
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
  pod 'GoogleSignIn'
  pod 'AmitySDK', '5.33.0'

  target 'google-identity-sign-inTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'google-identity-sign-inUITests' do
    # Pods for testing
  end

end
